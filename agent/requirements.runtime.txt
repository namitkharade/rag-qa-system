# Python dependencies for agent runtime (without heavy ingestion dependencies)
# Removed: unstructured[pdf], pypdf, and ingestion-only tools

# LangChain core - for agent logic and LLM interactions
langchain==0.1.20
langchain-openai==0.1.1
langchain-community==0.0.38
langchain-core==0.1.52
langsmith==0.1.57
langgraph==0.0.34

# Vector database client (lightweight, for queries only)
chromadb==0.4.24

# Configuration and validation
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# FastAPI server
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Geometry tool and Redis cache
shapely==2.0.2
redis==5.0.1
